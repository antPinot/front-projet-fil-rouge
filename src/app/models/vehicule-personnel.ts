export interface VehiculePersonnel {
    id?: number,
    immatriculation?: string,
    marque?: string,
    modele?: string,
    places?: number,
    limitePlaces?: number,
    collaborateursId?: number[]
}
